{
  "name": "github-blog",
  "version": "1.0.0",
  "description": "Jekyll Chirpy Blog for Cloudflare Pages",
  "scripts": {
    "build": "bundle exec jekyll build",
    "serve": "bundle exec jekyll serve",
    "install-deps": "bundle install"
  },
  "engines": {
    "ruby": ">= 3.0.0"
  }
}
